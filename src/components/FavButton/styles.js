import styled from 'styled-components';

export const Button = styled.button`
  display: flex;
  align-items: center;
  padding-top: 8px;
  background: transparent;
  border: none;
  outline: none;
  & svg {
    margin-right: 4px;
  }
`
